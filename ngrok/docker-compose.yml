services:
  ngrok:
    image: ngrok/ngrok:latest
    restart: unless-stopped
    command: http ${NGROK_TARGET_URL}
    environment:
      NGROK_AUTHTOKEN: ${NGROK_AUTHTOKEN}
    ports:
      - "4040:4040" # ngrok web inspector UI
